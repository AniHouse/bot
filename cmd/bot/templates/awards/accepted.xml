<template>
    <embed color="#ee281f">
        <title>Заявка одобрена</title>
        Выдача наград завершена
        {{ if .Failed }}
        [EOL]
        Однако, пользователи {{ .Failed }} по каким-то причинам не смогли получить награду.
        Награда начислится им позже автоматически.
        {{end}}
    </embed>
</template>